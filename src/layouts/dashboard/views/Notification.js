import React from 'react';
import { useParams } from 'react-router-dom';
import Notification from '../../../components/screens/dashboard/notifications/Notification';

export const ReadNotifications = () => {
    return (
        <Notification />
    );
}